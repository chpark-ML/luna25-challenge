# specify default configuration
defaults:
  - loader: luna25
  - processor: default
  - models: nodule_attr_seg.yaml

run_name: "eval_KD_bef"
run_modes:
  - test

sanity_check: False

hydra:
  run:
    dir: ./outputs/inference_results
  job:
    chdir: True  # keep hydra = 1.1 change directory behavior
