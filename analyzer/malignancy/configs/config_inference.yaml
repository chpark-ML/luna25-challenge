# specify default configuration
defaults:
  - loader: default
  - processor: default
  - models: 5_0_0rc2.yaml

run_name: "5_0_0rc2"
run_modes:
  - test
sanity_check: False

hydra:
  run:
    dir: ./outputs/inference_results
  job:
    chdir: True  # keep hydra = 1.1 change directory behavior
