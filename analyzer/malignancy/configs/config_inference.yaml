# specify default configuration
defaults:
  - loader: default
  - processor: default
  - models: 4_0_1rc3.yaml

run_name: "4_0_1rc3"
run_modes:
  - test
sanity_check: False

hydra:
  run:
    dir: ./outputs/inference_results
  job:
    chdir: True  # keep hydra = 1.1 change directory behavior
