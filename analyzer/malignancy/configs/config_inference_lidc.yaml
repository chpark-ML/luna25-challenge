# specify default configuration
defaults:
  - loader: lidc
  - processor: default
  - models: 5_3_0rc1.yaml

run_name: "5_3_0rc1"
run_modes:
  - test

sanity_check: False

hydra:
  run:
    dir: ./outputs/inference_results_lidc
  job:
    chdir: True  # keep hydra = 1.1 change directory behavior
